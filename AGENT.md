# Workout Tracker 协作规则

## 每次回答前

在本仓库开展工作时，每次回答和执行操作前都必须参考本文件，并遵守根目录 `AGENTS.md` 的开发约束。

## 所有修改必须对齐权威文档

所有修改（包括代码、配置、测试和文档）都必须先核对并对齐以下文件中的相关规定：

- `/home/chenyy/Workout-Tracker/requirements.md`：产品范围、业务行为、角色和权限。
- `/home/chenyy/Workout-Tracker/database.md`：数据实体、字段、关系、枚举和数据不变量。

如果用户命令与上述任一文件存在冲突，必须停止相关修改，向用户说明具体冲突、对应条款和影响，并询问用户如何处理。在用户明确决定前，不得直接执行冲突命令，也不得自行修改基线文档来消除冲突。如果两个基线文件之间存在影响本次任务的矛盾，也必须先询问用户。

用户明确批准变更基线后，才可按确认的范围同步文档和实现。

## 不清楚时先问，清楚后再实现

如果对需求或实现方式有不清楚的地方，必须直接向用户提问，不得凭猜测补全或擅自选择方案后执行。

在开始实现前，必须明确业务目标、输入输出、权限、状态变化、数据持久化方式、影响范围和验收标准中与任务有关的事项。用户回答后如果仍有疑问，继续询问，直到实现要求非常清晰再执行。

澄清期间可以只读检查相关文档和代码，以便提出准确的问题；不得开始依赖未明确事项的修改。

## 沿用当前项目分层框架

执行时必须参考 `/home/chenyy/Workout-Tracker/workout_tracker_project_review.md` 中的项目分层框架，按以下顺序理解和定位改动：

```text
业务场景 → 数据模型 → 后端分层 → API 契约 → 前端页面与状态
```

沿用现有前后端分离的模块化单体架构：

- Router：接收和解析 HTTP 请求，获取依赖，调用 Service，返回响应。
- Service：处理权限、业务规则、状态流转和用例编排，拥有事务提交与回滚边界。
- Domain Helper：承载必要的可复用领域逻辑，不拥有事务提交与回滚边界。
- Repository / Query：封装数据库访问与聚合查询，不承载完整业务流程，不提交或回滚事务。
- Model：定义与数据库基线一致的数据模型。
- Schema：定义 API 请求和响应的数据格式。
- 前端：按现有路由、业务域 API、共享客户端、全局上下文和 UI 组件分工组织，不复制后端权威业务规则。

审查文档用于指导理解和分层，其中的示例实体、接口和代码不能替代权威业务、数据及 API 契约；事务等具体技术约束继续遵守 `AGENTS.md` 和 `tech_stack.md`。不得为了快速实现而绕过、合并或擅自改变现有层级。如本次实现涉及无法消解的文档或架构冲突，必须先停止并询问用户。
