# 手机端球队管理 App MVP 需求文档

## 1. 文档基线

本文档描述手机端球队管理App的MVP产品范围。

数据实体、字段、枚举和关系以database.md为唯一基线

## 2. 产品目标

- 用户通过邮箱注册和登录，并维护个人资料。
- 用户可按名称搜索尚未加入的球队并提交入队申请，申请提交后创建状态为 `pending` 的成员关系。
- 球队管理员维护当前球队内的成员关系。
- 球队管理员填写训练或比赛活动表单，并在发布时创建活动。
- 队员在 Inbox 中接收通知，需要选择参加、不参加或待定。
- 比赛期间球队管理员记录进球、黄牌、红牌和换人，队员可以实时查看这些信息。
- 活动到达结束时间后自动完成，并按报名 `going` 结算给队员发放所属球队的金币。
- 队员使用所属球队的金币兑换球队商品。


## 3. 角色与权限

### 3.1 账号与球队角色

`User` 是独立于球队的应用账号，用户在球队内的角色可分为两种：

- `member`：普通队员。
- `admin`：球队管理员。

同一用户可在不同球队拥有不同角色。新建成员关系时默认角色为 `member`。

注：业务上真实球队队长仍使用 `member` 账号逻辑：可以报名训练/比赛、进入统计和获得奖励。`admin` 是管理活动、成员、金币和商店的后台管理账号，不能报名参加自己管理的活动，也不会进入报名榜统计或获得活动奖励。

### 3.2 普通队员

- 查看自己所属球队的信息、成员和活动。
- 查看 Inbox 通知并维护自己的活动报名状态。
- 查看比赛实时记录、报名情况、球队金币和兑换记录；普通队员的比赛实时看板为只读。
- 使用本球队金币兑换本球队商品，提交订单。

### 3.3 球队管理员

球队管理员不继承队员的报名和奖励能力；管理员账号用于管理当前球队，可以：

- 更新本球队资料。
- 创建、编辑和删除训练或比赛。
- 查看报名情况和活动完成后的结算结果。
- 在比赛进行期间新增或删除比赛实时记录，记录中保留操作者身份。
- 管理本球队的金币规则和商品。
- 确认或取消兑换订单。
- 审核本球队状态为 `pending` 的入队申请，并批准或拒绝申请。
- 管理已加入球队的成员关系。
- 将成员调整为 `member` 或 `admin`。
- 停用成员关系或归档当前球队。


## 5. 组织、球队与成员

### 5.1 组织

组织用于容纳一个或多个球队，包含名称、唯一slug和logo。
> 如新雅书院可以下设 男足、女足……

### 5.2 球队

球队必须属于一个组织，包含名称、简介、logo和状态：

- `active`
- `archived`

> 球队主页从“我的 → 我的球队 → 球队主页”进入，“我的”页不再提供直达球队主页的独立入口。球队主页展示球队资料、成员数和管理员列表，并保留管理员的球队资料管理功能；不展示我的金币、出勤概览、近期活动，以及底部成员、活动、出勤榜、Inbox、商店、金币跳转入口。后端球队主页聚合接口保留成员数、近期活动、出勤概览和金币概览等现有字段，不在 `Team` 中保存重复统计字段。

### 5.3 成员关系

`TeamMembership` 表示用户与球队的唯一成员关系：

- 角色：`member`、`admin`。
- 状态：`active`、`inactive`、`pending`。
- TeamMembership属性：球衣号码、球员姓名、加入时间（和离开时间）。

用户尚未加入目标球队时，可以按球队名称搜索球队并提交入队申请。若用户当前没有任何状态为 `active` 的 `TeamMembership`，登录后的无球队空状态必须提供球队搜索和入队申请入口。提交成功后，系统为该用户和球队创建角色为 `member`、状态为 `pending` 的 `TeamMembership`。

`pending` 成员关系仅表示入队申请，不能查看球队私有内容或参与球队活动。球队管理员批准后，系统将其状态更新为 `active`；管理员拒绝后，系统将其状态更新为 `inactive`。只有状态为 `active` 的成员关系才代表用户已经加入球队。

同一用户与同一球队只能存在一条 `TeamMembership`。已有 `pending` 或 `active` 成员关系时不得重复申请；已有 `inactive` 成员关系时再次申请，应复用该记录并将其更新为 `pending`，不得创建第二条记录。重复审批必须幂等返回当前成员关系状态。

## 6. 活动、报名与完成结算

### 6.1 活动

训练、比赛和其他活动统一使用 `Event`：

- 类型：`training`、`match`、`other`。
- 状态：`published`、`completed`。
- 通用字段：标题、描述、地点、开始时间、结束时间。
- 时间关系：结束时间必填，并且必须晚于开始时间。

> MVP手机端提供训练和比赛创建入口；`other` 保留为数据模型扩展能力。

### 6.2 比赛详情

比赛通过一对一的 `MatchDetails` 保存对手、双方比分、比赛结果和备注。训练和其他活动不得创建 `MatchDetails`。

创建比赛活动时必须填写比赛对手。比分和结果可以在比赛完成前确认；双方比分必须成对填写，胜平负结果必须与比分一致。

### 6.3 活动状态规则

- `published`：球队有效成员可查看和报名；比赛可在比赛开始时间之后记录实时事件；重复发布请求必须幂等返回且不重复通知。
- `completed`：报名和比赛记录停止修改，可以查看最终报名、奖励和汇总。


允许的主流程为：

```text
published → completed

published → hard delete
```

管理员填写活动表单期间不创建 `Event` 记录。只有管理员点击发布后，后端才创建 `status=published` 的活动，并立即创建对应 `new_event` 通知。发布 `training` 或 `match` 活动前，后端必须检查当前球队是否已有对应的 `active` `CoinRule`；缺少规则时拒绝发布，并要求管理员先配置金币规则。活动到达 `end_time` 后，由后端定时任务或等效机制自动将活动转为 `completed` 并执行奖励结算，不需要管理员确认。

### 6.4 报名

`EventSignup` 表示参与意愿，状态为：

- `going`
- `not_going`
- `maybe`

默认状态为 `maybe`。选择 `not_going` 时必须在 `note` 中填写原因。每名用户对同一活动最多一条报名记录。

队员只能在活动为 `published` 时修改自己的报名。活动 `completed` 后不能再修改。

### 6.5 完成活动与奖励结算

MVP 不维护独立的出勤/考勤实体。活动到达 `end_time` 后，后端自动将活动从 `published` 转为 `completed`，并按报名情况自动结算金币，不需要管理员确认：

- 有效成员范围按活动开始时的队员资格判断：`role=member`，`joined_at <= event.start_time`，且当前仍为 active。
- 无报名记录的成员按 `maybe` 处理；不自动补写 absent。
- 仅 `going` 报名按球队有效规则发放 `signup_reward`（训练触发 `training_signup`，比赛触发 `match_signup`）。
- 同一活动、同一用户的报名奖励只能发放一次。
- 活动完成后不能修改或删除活动安排，也不能再通过出勤接口修正奖励。

## 7. 比赛实时记录

### 7.1 记录类型

- 
- `goal`
- `yellow_card`
- `red_card`
- `substitution`

> 这里目前主要用来记录友谊赛战况

### 7.2 字段规则

- 进球和牌罚要求填写比赛分钟、队员姓名和号码。
- 换人要求填写比赛分钟、换出队员姓名和号码、换入队员姓名和号码。
- 所有记录保存活动 ID、创建人和创建时间。
- 只有球队管理员可以新增或删除比赛实时记录。
- 只有 `type=match` 且 `status=published` 的活动可以新增记录。
- 活动完成后记录只读，避免赛后数据被静默覆盖。
- 客户端为每一条新增记录（比如红牌、换人）提交 UUID `id`；相同 `id` 和完全一致 payload 的重试必须幂等返回已有记录，相同 `id` 但内容不一致必须返回冲突，避免重复记录。

实时看板在 MVP 阶段使用短轮询更新。

## 8. 金币与奖励

### 8.1 奖励规则

`CoinRule` 属于球队，支持以下触发类型：

- `training_signup`
- `match_signup`
- `manual`

奖励金额由后端根据球队有效规则计算，移动端不得提交奖励金额。训练和比赛报名奖励金额由球队管理员在球队设置中配置，金额可以设为 0。

每个球队最多只能有一条 `active` `training_signup` 规则和一条 `active` `match_signup` 规则。发布 `training` 或 `match` 活动前，后端必须确认对应 `active` 规则存在。活动自动完成时，后端根据 `Event.type` 自动选择对应的 `active` 规则进行结算，不等待管理员选择规则。

> 这里的契约是用户端仅提供报名信息，由后端统一根据报名情况进行奖励分发；否则会导致用户直接提交奖励金额的作弊行为

### 8.2 金币流水

每次余额变化必须新增一条 `CoinTransaction`，类型为：

- `signup_reward`
- `redemption`
- `admin_adjustment`
- `other_reward`
- `refund`

金币属于 `(user, team)`，权威余额为该用户在该球队全部流水金额之和。不得把 `User` 上的可变余额作为权威来源，不得静默修改历史流水。

> 系统真正认可的余额，不是某个 User.balance 字段，而是把这个用户在这个球队的所有 CoinTransaction.amount 加起来

同一活动、同一用户的报名奖励只能发放一次。
创建金币规则由客户端提交 UUID `id`，相同 `id` 和相同 payload 的重试必须幂等返回已有规则，相同 `id` 被不同规则内容复用必须返回冲突，避免重复创建训练/比赛报名奖励配置。

## 9. 商店与兑换

### 9.1 商品

`StoreItem` 属于球队，包含名称、简介、图片、金币价格、库存、上下架状态和创建人。

- `is_active=true` 才可兑换。
- `stock=NULL` 表示不限库存。
- 价格和库存由球队管理员维护。
- 创建商品由客户端提交 UUID `id`，相同 `id` 和相同 payload 的重试必须幂等返回已有商品，相同 `id` 被不同商品内容复用必须返回冲突，避免重复上架。

### 9.2 兑换

`Redemption` 保存用户、球队、商品、数量、成交单价、成交总价和状态：

- `pending`
- `fulfilled`
- `cancelled`
- `refunded`

兑换时必须在同一个数据库事务中完成余额校验、库存校验、订单创建、金币扣减和库存扣减。订单保存成交时价格，商品后续改价不能影响历史订单。

`pending` 订单在创建时已扣币并预留有限库存。履约成功后进入 `fulfilled`，重复履约应幂等返回且不重复通知。待处理订单被取消时进入 `cancelled`，同时新增 `refund` 金币流水并恢复库存；已履约订单退款时进入 `refunded`，同样使用补偿性 `refund` 流水，不修改原扣币流水。

## 10. Inbox 与移动通知

`Notification` 是 Inbox 的权威数据源，每条通知属于一个用户并独立保存已读状态。活动发布入库时，后端立即为球队有效队员创建通知。修改和删除活动不再额外创建活动通知。MVP 使用以下类型：

- `new_event`
- `coin_earned`
- `redemption_completed`
- `team_announcement`

当前阶段仅使用 App 内 Inbox 通知，不接入手机系统推送。收件箱和个人页不提供设备通知设置，不申请系统通知权限，不在启动或回到前台时获取、注册或刷新设备推送令牌。

`DeviceToken` 表及设备 API 暂时保留供后续接入；后端远程推送保持关闭。后续启用时，一名用户可以拥有多个设备，推送失败不得回滚已经成功的核心业务事务。

## 11. 页面范围

### 11.1 通用与队员页面

- 登录 / 注册
- 首页与球队切换
- 无球队空状态、球队名称搜索与入队申请状态
- 球队主页和成员列表
- Inbox
- 活动列表与详情
- 比赛实时看板和记录页
- 报名榜
- 金币余额与流水
- 商品列表、详情和兑换记录
- 个人资料

报名榜（移动端显示为“出勤榜”）使用两行筛选按钮：第一行为周榜、月榜、总榜，第二行为类型：训练、比赛。默认总榜且两种类型均选中；类型允许多选，但至少保留一种。周榜按设备当地时间的本周（周一开始）计算，月榜按当地自然月计算，总榜不限制日期，均按活动开始时间筛选且只统计已完成活动。双选时合并统计每位队员的次数和参加比例，沿用现有报名、成员资格和排序规则。切换按钮立即重新请求后端统计，无需手动提交筛选。

页面间导航的左上角返回按钮统一只显示箭头，不显示返回文字或路由路径。

所有日期时间输入（活动创建、编辑、活动筛选及金币流水筛选）统一拆成日期和时间选择：日期按年、月、日滚动选择，时间使用 24 小时制，每 30 分钟一项（00:00 至 23:30）。输入与回显固定使用北京时间（UTC+08:00），不跟随设备所在时区；提交仍使用既有 UTC API 时间格式。已有非整点/半点时间保留原值；只改日期保留原时分秒，重新选择并确认时间时才采用半小时间隔。取消选择不修改原值；筛选条件可以清空。活动结束时间必填且晚于开始时间。

页面加载统一区分未请求、加载中、成功和失败。按单轮请求结果判断，不自动重试：请求成功且对应列表为空时，只显示空数据提示，不显示重试按钮；请求失败不能根据初始化的空列表显示“暂无数据”。断网、超时及 HTTP 408、429、500、502、503、504 提供失败提示和手动重试；认证、权限、参数、资源不存在等错误显示对应提示或必要操作入口，不显示通用重试。HTTP 成功但响应无效也属于失败，不得当作空数据。操作成功、表单提示与加载失败分开；同页独立列表按各自请求结果展示。加载中不显示上一轮空数据或错误提示。

### 11.2 球队管理员页面

- 球队资料管理
- 入队申请审核
- 成员与角色管理
- 创建和编辑训练 / 比赛
- 活动报名查看与完成结算结果
- 比赛详情和汇总
- 金币规则管理
- 商品与兑换订单管理

创建活动使用独立页面；活动页和球队活动管理页的创建入口跳转至同一页面，不在列表与筛选栏之间展开表单。进入页面先核对目标球队的管理员身份，保留训练/比赛字段与北京时间选择器；创建成功进入活动详情，返回活动列表时刷新数据。

## 12. 核心流程

### 12.1 入队

1. 用户通过认证服务注册。
2. 后端使用认证身份创建或同步 `User`。
3. 用户尚未加入目标球队时，通过球队名称搜索球队并提交入队申请。
4. 后端创建角色为 `member`、状态为 `pending` 的 `TeamMembership`；此时用户尚未获得球队访问和活动参与权限。
5. 目标球队的管理员查看申请并进行审批：批准时将成员关系更新为 `active`，拒绝时更新为 `inactive`。
6. 管理员可将状态为 `active` 的成员角色调整为 `member` 或 `admin`。

### 12.2 活动

1. 球队管理员在客户端填写活动表单，填写期间不创建 `Event` 记录；点击发布后，后端先检查该类型活动所需的 `active` `CoinRule` 是否存在，检查通过后创建 `status=published` 的活动，并自动为球队有效队员创建 `new_event` 通知。客户端提交活动 UUID，重复提交相同 `id` 和相同 payload 必须幂等返回且不重复通知。
2. 比赛同时创建 `MatchDetails`；创建比赛同样使用嵌套 `event.id` 幂等，重复提交不得重复创建比赛详情或通知。
3. 队员通过发布活动时收到的 Inbox 通知或活动列表进入详情。
4. 球队管理员实际修改已发布活动时不额外创建活动通知，但根据修改后的内容同步已发送通知中的具体信息；重复提交相同更新必须幂等返回当前活动。
5. 球队管理员删除未完成活动时，后端先锁定目标活动，再物理删除活动及其从属记录；删除不额外创建活动通知，但撤销此前活动发布时创建的通知。
6. 队员提交或修改 `EventSignup`。
7. 活动到达 `end_time` 后自动完成：后端按活动开始时的历史有效队员资格，以报名 `going` 自动发放 `signup_reward` 并生成通知，不等待管理员确认；结算结果包含 `going_count`。

### 12.3 公告

球队管理员发布球队公告时，客户端提交公告 UUID `id`。后端为球队有效队员创建 `team_announcement` 通知，并把该 `id` 保存为通知 `reference_id`；重复提交相同 `id` 和相同内容必须幂等返回已有通知，不重复创建 Inbox 或 Push，相同 `id` 被不同公告内容复用必须返回冲突。

### 12.4 兑换

1. 队员选择本球队有效商品。
2. 后端在事务中校验球队成员身份、余额和库存。
3. 创建 `Redemption`，写入负数 `CoinTransaction` 并扣减库存。
4. 球队管理员完成订单后，将状态改为 `fulfilled` 并通知用户。

## 13. 验收标准

- 用户完成注册后可以按名称搜索球队并提交入队申请，提交后生成状态为 `pending` 的 `TeamMembership`。
- 只有管理员批准并将成员关系更新为 `active` 后，用户才能进入球队；`pending` 和 `inactive` 状态均不能访问球队私有内容或参加活动。
- 同一用户不能对同一球队重复创建成员关系，重复审批不会重复创建或错误变更成员关系。
- 同一用户可在不同球队拥有不同角色和球衣号码。
- 球队管理员可以发布、修改、删除未完成的训练或比赛；活动到达结束时间后由后端自动完成。
- 球队管理员不能报名活动、进入报名统计或获得活动报名奖励；真实球队队长按队员账号逻辑使用 `member`。
- `not_going` 报名必须填写原因。
- 活动自动完成时按报名 `going` 结算，不维护独立出勤记录，也不自动补 absent。
- 完成后的活动禁止队员修改报名，且禁止修改或删除活动安排。
- 发布中的比赛可以记录四类比赛事件，完成后只读。
- 报名奖励不可重复发放，金币余额与流水汇总一致；手工调整允许余额变为负数。
- 兑换不会出现库存超卖或重复扣减；余额不足时不得创建兑换。
- 历史订单保留成交价格。
- 不同球队的数据和金币严格隔离。
- iOS 和 Android 真机可以完成核心闭环。

## 14. 实施前配置项

以下内容是部署配置，不改变数据库模型：

- 初始组织、球队和管理员账号。
- 默认训练、比赛报名金币奖励金额；之后由球队管理员在球队设置中调整。
- 商品兑换后的线下履约说明。
