# 手机端球队管理 App MVP 需求文档

## 1. 文档基线

本文档描述手机端球队管理 App 的 MVP 产品范围。

数据实体、字段、枚举和关系以 [database.md](database.md) 为唯一基线。本文件不定义数据库中不存在的角色、状态或核心实体。

## 2. 产品目标

- 用户通过邮箱注册和登录，并维护个人资料。
- 管理员维护当前球队内的成员关系。
- 队长创建、发布、修改、删除并完成训练或比赛活动。
- 队员在 Inbox 中接收通知，并选择参加、不参加或待定。
- 比赛期间记录进球、黄牌、红牌和换人。
- 活动完成时确认实际出勤并发放球队金币。
- 队员使用所属球队的金币兑换球队商品。

MVP 采用手机端优先、后端权威、模块化单体的实现方式。

## 3. 角色与权限

### 3.1 账号与球队角色

`User` 是独立于球队的应用账号，不保存全局业务角色。用户加入球队后，通过 `TeamMembership.role` 获得该球队内的角色：

- `member`：普通队员。
- `captain`：队长。
- `admin`：管理员。

同一用户可在不同球队拥有不同角色。新建成员关系时默认角色为 `member`。

### 3.2 普通队员

- 查看自己所属球队的信息、成员和活动。
- 查看 Inbox 通知并维护自己的活动报名状态。
- 查看比赛实时记录、出勤记录、球队金币和兑换记录；普通队员的比赛实时看板为只读。
- 使用本球队金币兑换本球队商品。

### 3.3 队长

除普通队员权限外，队长可以：

- 更新本球队资料。
- 创建、编辑、发布、删除和完成训练或比赛。
- 查看报名情况并确认实际出勤。
- 在活动完成后修正出勤；修正会触发金币补发或追回。
- 在比赛进行期间新增或删除比赛实时记录，记录中保留操作者身份。
- 管理本球队的金币规则和商品。
- 确认或取消兑换订单。

### 3.4 管理员

除队长权限外，管理员可以：

- 管理球队成员关系。
- 按姓名、邮箱或学号搜索已同步用户，并添加为当前球队成员。
- 将成员调整为 `member`、`captain` 或 `admin`。
- 停用成员关系或归档当前球队。

第一个 `Organization`、`Team` 和管理员成员关系由部署初始化流程创建。MVP 中管理员权限限定在当前球队内，不提供组织级跨球队管理权限。

## 4. MVP 范围

### 4.1 本期范围

- 邮箱注册、登录、退出和个人资料。
- 组织与球队基础信息展示。
- 球队成员与球队内角色管理。
- 训练、比赛的创建、发布、修改、删除、报名和完成。
- Inbox 通知和移动设备推送令牌管理。
- 比赛实时记录。
- 报名意愿和实际出勤分离记录。
- 出勤榜和比赛汇总。
- 可配置的球队金币奖励规则。
- 不可静默修改的金币流水。
- 球队商品和兑换订单。

### 4.2 非本期范围

- 队伍发现、跨队联系和友谊赛邀约。
- 场地抽签和场地表管理。
- 社交广场、聊天和内容流。
- 联赛积分、裁判和复杂赛程编排。
- 第三方支付、电商物流和实物发货闭环。
- 微服务、多区域部署和复杂消息基础设施。

## 5. 组织、球队与成员

### 5.1 组织

组织用于容纳一个或多个球队，包含名称、唯一 slug 和可选 logo。

### 5.2 球队

球队必须属于一个组织，包含名称、简介、可选 logo 和状态：

- `active`
- `archived`

球队主页展示的信息均从现有实体聚合获得，包括队长列表、成员数、近期活动、出勤概览和金币概览，不在 `Team` 中保存重复统计字段。

### 5.3 成员关系

`TeamMembership` 表示用户与球队的唯一成员关系：

- 角色：`member`、`captain`、`admin`。
- 状态：`active`、`inactive`、`pending`。
- 球队属性：球衣号码、位置、加入和离开时间。

只有有效成员才能查看球队私有内容或参与球队活动。

## 6. 活动、报名与出勤

### 6.1 活动

训练、比赛和其他活动统一使用 `Event`：

- 类型：`training`、`match`、`other`。
- 状态：`draft`、`published`、`completed`、`cancelled`。
- 通用字段：标题、描述、地点、开始时间、结束时间、报名截止时间和创建人。
- 时间关系：若填写结束时间，必须晚于开始时间；若填写报名截止时间，不得晚于开始时间。

MVP 手机端提供训练和比赛创建入口；`other` 保留为数据模型扩展能力。

### 6.2 比赛详情

比赛通过一对一的 `MatchDetails` 保存对手、双方比分、比赛结果和备注。训练和其他活动不得创建 `MatchDetails`。

比赛对手在发布前必须填写。比分和结果可以在比赛完成前确认；双方比分必须成对填写，胜平负结果必须与比分一致。

### 6.3 活动状态规则

- `draft`：仅队长和管理员可查看和编辑。
- `published`：球队有效成员可查看和报名；比赛可记录实时事件；重复发布必须幂等返回且不重复通知。
- `completed`：报名和比赛记录停止修改，可以查看最终出勤、奖励和汇总。
- `cancelled`：保留为数据模型状态，但 MVP 删除活动时采用物理删除，不以 cancelled 表示删除。

允许的主流程为：

```text
draft → published → completed

draft/published → hard delete
```

### 6.4 报名

`EventSignup` 表示参与意愿，状态为：

- `going`
- `not_going`
- `maybe`

默认状态为 `maybe`。选择 `not_going` 时必须在 `note` 中填写原因。每名用户对同一活动最多一条报名记录。

队员只能在报名截止前且活动为 `published` 时修改自己的报名。活动 `completed` 后不能再修改。

### 6.5 实际出勤

`Attendance` 与报名意愿必须分开，状态为：

- `present`
- `late`
- `absent`
- `excused`

每名用户对同一活动最多一条出勤记录。记录必须保存 `recorded_by`、`recorded_at` 和可选说明。

活动完成时，队长或管理员确认出勤并进行奖励结算。活动开始时有资格参与且缺失的出勤记录会自动写为 `absent`：成员必须满足 `joined_at <= event.start_time`，且当前仍为 active 或 `left_at >= event.start_time`。活动后才加入的成员不会被自动补 absent；活动时仍在队但完成前离队的成员会被自动补 absent。活动完成后不能修改或删除活动安排，但可以修正出勤；出勤修正会根据球队金币规则补发或追回金币，余额允许变为负数。

## 7. 比赛实时记录

### 7.1 记录类型

- `goal`
- `yellow_card`
- `red_card`
- `substitution`

### 7.2 字段规则

- 进球和牌罚要求填写比赛分钟、队员姓名和号码。
- 换人要求填写比赛分钟、换出队员姓名和号码、换入队员姓名和号码。
- 所有记录保存活动 ID、创建人和创建时间。
- 只有队长或管理员可以新增或删除比赛实时记录。
- 只有 `type=match` 且 `status=published` 的活动可以新增记录。
- 活动完成后记录只读，避免赛后数据被静默覆盖。
- 客户端为新增记录提交 UUID `id`；相同 `id` 和完全一致 payload 的重试必须幂等返回已有记录，相同 `id` 但内容不一致必须返回冲突，避免重复记录。

实时看板在 MVP 阶段使用短轮询更新。

## 8. 金币与奖励

### 8.1 奖励规则

`CoinRule` 属于球队，支持以下触发类型：

- `training_attendance`
- `match_attendance`
- `late_attendance`
- `manual`

奖励金额由后端根据球队有效规则计算，移动端不得提交权威奖励金额。训练、比赛和迟到奖励金额由队长或管理员在球队设置中配置，金额可以设为 0。

### 8.2 金币流水

每次余额变化必须新增一条 `CoinTransaction`，类型为：

- `attendance_reward`
- `redemption`
- `admin_adjustment`
- `other_reward`
- `refund`

金币属于 `(user, team)`，权威余额为该用户在该球队全部流水金额之和。不得把 `User` 上的可变余额作为权威来源，不得静默修改历史流水。

同一活动、同一用户的出勤奖励只能发放一次。
创建金币规则由客户端提交 UUID `id`，相同 `id` 和相同 payload 的重试必须幂等返回已有规则，相同 `id` 被不同规则内容复用必须返回冲突，避免重复创建训练/比赛/迟到奖励配置。

## 9. 商店与兑换

### 9.1 商品

`StoreItem` 属于球队，包含名称、简介、图片、金币价格、库存、上下架状态和创建人。

- `is_active=true` 才可兑换。
- `stock=NULL` 表示不限库存。
- 价格和库存由队长或管理员维护。
- 创建商品由客户端提交 UUID `id`，相同 `id` 和相同 payload 的重试必须幂等返回已有商品，相同 `id` 被不同商品内容复用必须返回冲突，避免重复上架。

### 9.2 兑换

`Redemption` 保存用户、球队、商品、数量、成交单价、成交总价和状态：

- `pending`
- `fulfilled`
- `cancelled`
- `refunded`

兑换时必须在同一个数据库事务中完成余额校验、库存校验、订单创建、金币扣减和库存扣减。订单保存成交时价格，商品后续改价不能影响历史订单。

`pending` 订单在创建时已扣币并预留有限库存。履约成功后进入 `fulfilled`，重复履约应幂等返回且不重复通知。待处理订单被取消时进入 `cancelled`，同时新增 `refund` 金币流水并恢复库存；已履约订单退款时进入 `refunded`，同样使用补偿性 `refund` 流水，不修改原扣币流水。

## 10. Inbox 与移动通知

`Notification` 是 Inbox 的权威数据源，每条通知属于一个用户并独立保存已读状态。创建活动、发布活动、修改已发布活动、删除已发布活动时，后端立即为球队有效成员创建通知；草稿创建通知保存活动标题和时间快照但不提供活动详情链接，草稿修改和草稿删除不通知。MVP 使用以下类型：

- `new_event`
- `event_updated`
- `event_deleted`
- `coin_earned`
- `redemption_completed`
- `team_announcement`

`DeviceToken` 保存 iOS 或 Android 推送令牌；一名用户可以拥有多个设备。推送失败不得回滚已经成功的核心业务事务，用户仍可在 Inbox 中查看通知。

## 11. 页面范围

### 11.1 通用与队员页面

- 登录 / 注册
- 首页与球队切换
- 球队主页和成员列表
- Inbox
- 活动列表与详情
- 比赛实时看板和记录页
- 出勤榜
- 金币余额与流水
- 商品列表、详情和兑换记录
- 个人资料与设备通知设置

### 11.2 队长和管理员页面

- 球队资料管理
- 成员与角色管理
- 创建和编辑训练 / 比赛
- 活动报名与实际出勤管理
- 比赛详情和汇总
- 金币规则管理
- 商品与兑换订单管理

## 12. 核心流程

### 12.1 入队

1. 用户通过认证服务注册。
2. 后端使用认证身份创建或同步 `User`。
3. 管理员创建 `TeamMembership`，默认角色为 `member`；重复添加同一用户且成员内容一致时幂等返回已有成员关系，内容不一致时返回冲突。
4. 管理员可将球队内角色调整为 `captain` 或 `admin`。

### 12.2 活动

1. 队长创建 `draft` 活动，并为球队有效成员创建不带活动详情链接的 `new_event` 快照通知；客户端提交活动 UUID，重复提交相同 `id` 和相同 payload 必须幂等返回且不重复通知。
2. 比赛同时创建 `MatchDetails`；创建比赛同样使用嵌套 `event.id` 幂等，重复提交不得重复创建比赛详情或通知。
3. 发布活动并为球队有效成员创建可打开活动详情的 `new_event` 通知。
4. 队长实际修改已发布活动时为球队有效成员创建 `event_updated` 通知；重复提交相同更新必须幂等返回且不重复通知。
5. 队长删除未完成且已发布活动时，后端先锁定目标活动，随后为球队有效成员创建 `event_deleted` 通知，再物理删除活动及其从属记录。
6. 队员提交或修改 `EventSignup`。
7. 队长确认实际 `Attendance`。
8. 完成活动，按活动开始时的历史有效成员资格自动补齐缺失 `absent`，生成奖励流水和通知。

### 12.3 公告

队长或管理员发布球队公告时，客户端提交公告 UUID `id`。后端为球队有效成员创建 `team_announcement` 通知，并把该 `id` 保存为通知 `reference_id`；重复提交相同 `id` 和相同内容必须幂等返回已有通知，不重复创建 Inbox 或 Push，相同 `id` 被不同公告内容复用必须返回冲突。

### 12.4 兑换

1. 队员选择本球队有效商品。
2. 后端在事务中校验球队成员身份、余额和库存。
3. 创建 `Redemption`，写入负数 `CoinTransaction` 并扣减库存。
4. 队长完成订单后，将状态改为 `fulfilled` 并通知用户。

## 13. 验收标准

- 用户可以登录并加入一个或多个球队。
- 同一用户可在不同球队拥有不同角色和球衣号码。
- 队长可以创建、发布、修改、删除未完成的训练或比赛，并完成已发布活动。
- `not_going` 报名必须填写原因。
- 报名与实际出勤分别存储和展示。
- 完成后的活动禁止队员修改报名，且禁止修改或删除活动安排。
- 发布中的比赛可以记录四类比赛事件，完成后只读。
- 出勤奖励不可重复发放，出勤修正会补发或追回金币，金币余额与流水汇总一致且允许为负数；完成活动中已有的历史考勤即使成员后来 inactive 也可修正，但不能为 inactive 成员新增历史考勤。
- 兑换不会出现库存超卖或重复扣减；余额不足时不得创建兑换。
- 历史订单保留成交价格。
- 不同球队的数据和金币严格隔离。
- iOS 和 Android 真机可以完成核心闭环。

## 14. 实施前配置项

以下内容是部署配置，不改变数据库模型：

- 初始组织、球队和管理员账号。
- 默认训练、比赛、迟到金币奖励金额；之后由队长或管理员在球队设置中调整。
- 商品兑换后的线下履约说明。
